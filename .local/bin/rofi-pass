#!/usr/bin/env bash
PASSWORD_STORE_DIR="$HOME/.password-store"

entry=$(find "$PASSWORD_STORE_DIR" -type f -name '*.gpg' \
  | sed "s|$PASSWORD_STORE_DIR/||;s|\.gpg$||" \
  | rofi -dmenu -p "pass:")

[ -z "$entry" ] && exit

pass -c "$entry"
