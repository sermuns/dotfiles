#!/usr/bin/env bash
PASSWORD_STORE_DIR="$HOME/.password-store"

entry=$(fd . -e gpg "$PASSWORD_STORE_DIR" \
  | sed "s|$PASSWORD_STORE_DIR/||;s|\.gpg$||" \
  | rofi -dmenu -p "pass:")

[ -z "$entry" ] && exit

pass -c "$entry"
