[mgr]
# show_hidden = true

[open]
rules = [
	{ mime = "text/*", use = "edit" },
	{ mime = "video/*", use = "play" },

	{ mime = "application/json", use = "edit" },

	{ mime = "text/html", use = [ "open", "edit" ] },
]
